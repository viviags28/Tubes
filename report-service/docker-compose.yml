version: '3.8'

services:

  report-service:
    build: .
    ports:
      - "8083:8083"
    depends_on:
      - mysql

  mysql:
    image: mysql:8
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: tubesdb
    ports:
      - "3306:3306"